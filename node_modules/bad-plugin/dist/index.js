"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

exports.default = function (_ref) {
  var t = _ref.types;

  return {
    visitor: {
      JSXAttribute: function JSXAttribute(path, state) {
        if (path.node.name.name === "wrapIf") {
          var jSXElement = path.parentPath.parent;
          var callExpression = t.callExpression(t.identifier("show"), []);
          var conditionalExpression = t.conditionalExpression(callExpression, jSXElement, t.nullLiteral());
          var jSXExpressionContainer = t.jSXExpressionContainer(conditionalExpression);

          path.parentPath.parentPath.replaceWith(conditionalExpression); // Errors with child

          path.remove();
        }
      }
    }
  };
};